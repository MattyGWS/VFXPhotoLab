# VFX Photo Lab roadmap

## Completed — Layer and Mask Workflow (0.4.0e.1)

- Native WebGPU/tiled image architecture, direct visible-tile presentation, progressive navigation and sparse raster history.
- Complete raster/adjustment/group mask lifecycle, mask painting, one-byte mask history and Mask channel.
- Recursive duplication, defensive project loading, thumbnail caching and hierarchy persistence.
- Isolated and Pass Through groups across CPU reference and native tiled GPU paths.
- Nested hierarchy cancellation, resource guards, accepted-image diagnostics and document-session stale-result rejection.

## Completed — Alpha-Safe Editable Channels (0.4.1)

- Direct greyscale editing targets for Red, Green, Blue and Alpha on Base Image and Raster layers.
- Straight RGBA8/RGBA64 component storage and alpha edits that retain hidden RGB.
- Component-only sparse history, optional Base Image pixel overrides and backwards-compatible project-format-6 persistence.
- Alpha-safe full-resolution export and exact hidden-RGB TGA round-trips.

## Completed — New Document Workflow (0.4.2)

- File → New Document with screen, texture and print presets, custom dimensions, orientation swap and resolution metadata.
- RGB/Grayscale modes, 8/16-bit integer precision, sRGB/Linear sRGB and configurable backgrounds.
- One directly paintable Background Base Image by default, grayscale-aware channels and project-format-6 metadata persistence.

## Completed — Multi-Document Workspace (0.4.3)

- **0.4.3a complete:** `DocumentSession` extraction, per-session Undo stack behind `QUndoGroup`, session-owned edit/selection/view/history state and unique render identity.
- **0.4.3b complete:** renderer activation, UUID+serial request validation, CPU/GPU tile namespaces, session-scoped invalidation and per-session accepted diagnostics.
- **0.4.3b.1 complete:** Layers-panel mask editing uses a proportional red overlay over the composite; Channels-panel Mask remains isolated black and white. The distinction is session-owned and Undo-safe.
- **0.4.3c complete:** private atomic exact-pixel session snapshots, Hot/Warm/Cold LRU residency, a shared decoded-document/history memory budget, session cache cleanup and hard-pressure inactive-history checkpointing.
- **0.4.3d complete:** compact bottom model/delegate thumbnail strip, multi-open, simultaneous Hot/Warm/Cold switching, per-document Undo/view/edit state, close/save workflows and keyboard navigation.
- **0.4.3d.1 complete:** first-open centring and durable Fit mode, plus a less aggressive Warm-session count guard while retaining the same global byte budget.
- **0.4.3d.2 complete:** zoom-independent transform snapping to document edges/centres, visible guides and other visible layer/group bounds, with multi-selection anchors, resize snapping, hysteresis, alignment indicators and Ctrl bypass.

## Completed — 0.5.0 Selections and Local Editing

- **0.5.0a complete:** one document-owned sparse 8-bit selection, active-empty distinction, compressed per-document selection history, marching-ants display, Select All/Deselect/Invert, edge visibility, public version-6 persistence and private Cold snapshot isolation.
- **0.5.0b complete:** grouped Rectangle/Ellipse Select tools, Replace/Add/Subtract/Intersect, anti-aliasing, fixed 1:1/from-centre geometry, live non-destructive preview, Space repositioning and Escape cancellation.
- **0.5.0b.1 complete:** smooth coalesced marquee preview, viewport-void deselection and compact right-click tool-family buttons.
- **0.5.0c complete:** Freehand and Polygonal Lasso with screen-space point sampling/simplification, live closure previews, click/keyboard completion, vertex removal, all four combine modes and sparse one-step history.
- **0.5.0d complete:** layer-alpha and stored-mask selection sources with all four combine modes; tiled Feather, Expand, Contract and Smooth; live non-destructive Refine Selection with Smooth, Feather, Contrast and Shift Edge.
- **0.5.0e complete:** selection-aware raster/channel/mask painting, active-empty stroke blocking, target-aware alpha-safe Clear, selection-derived masks and adjustment-layer initialisation, and focus-aware Delete routing.
- **0.5.0f complete:** Selection-local move/resize/rotate for raster pixels, direct channels and masks, with selection-following Undo plus fixes for cropped raster Paste scale and direct-target Copy/Cut/Paste routing.
- **0.5.0f.1 complete:** automatic selected-pixel Transform scope, repaired RGB-channel clipboard targeting, Ctrl-click alpha/mask selection loading, Alt-drag layer duplication and consistent canvas Delete routing.
- **0.5.0g complete:** Copy, Cut, Paste and Copy Merged across raster pixels, editable channels, masks and documents using a private alpha-safe payload plus normal system image interoperability, original internal placement and sparse one-step Undo.
- **0.5.0h complete:** native tiled selection-aware raster/mask brush clipping, straight-RGBA hidden-colour preservation, selected-layer channel-view isolation, stable Tool Options height, stricter stale-result publication guards and persistence validation hardening.

## In progress — 0.6.0 Retouching Tools

- **0.6.0a complete:** Clone Stamp interaction and authoritative CPU reference with Alt-click sampling, Aligned/non-aligned offsets, Current Target/Composite sources, selection-aware raster/channel/mask targets, alpha-aware soft-edge transfer into straight RGBA8/RGBA64 storage, sparse history and per-document transient source state.
- **0.6.0b complete:** native tiled GPU Clone Stamp final commits with bounded cross-tile source patches and bilinear halos, selection-aware straight-RGBA/channel/mask WGSL, whole-stroke CPU fallback, startup parity and optional transformed multi-tile tests.
- **0.6.0b.1 complete:** floating-point whole-stroke Clone coverage accumulation removes low-opacity chromatic contouring in CPU, GPU and live preview while retaining per-dab build-up semantics.
- **0.6.0c.1 complete:** sampled Healing Brush with sparse Poisson seamless cloning, aggressive source-structure transfer, boundary-matched colour/lighting, raster-Alpha preservation, selection-aware sparse history, 8/16-bit CPU reference and independent per-document source state.
- **0.6.0d complete:** automatic Spot Healing with deterministic non-overlapping nearby-source search, Current Layer/Composite sources, boundary/texture/Alpha/distance scoring, Poisson seamless cloning, selection-aware sparse history and asynchronous stale-result rejection.
- **0.6.0d.1 complete:** selection-aware red treatment feedback for deferred Spot Healing plus zero-resample/lazy-serialisation/in-process-cache/snapshot-history fast paths for full-document Copy and Paste.
- **0.6.0e complete:** selection-driven Source/Destination Patch Tool with live selection-shaped drag preview, Current Layer/Composite sampling, feathered sparse Poisson blending, exact destination-Alpha preservation and atomic pixel-plus-selection Undo.
- **0.6.0f complete:** grouped Dodge/Burn/Sponge brushes with tonal-range or saturation modes, tone/vibrance protection, selection-aware raster/channel/mask targeting where meaningful, exact raster-Alpha preservation and sparse CPU history.
- **0.6.0f.1 complete:** sparse incremental live-preview coverage and renderer-reference release keep Dodge/Burn/Sponge responsiveness stable throughout long gestures while remaining pixel-identical to whole-stroke commits.
- **0.6.0g complete:** grouped Blur/Sharpen detail brushes with alpha-aware neighbourhood filtering, bounded highlight-protected unsharp masking, raster/channel/mask targets, RGBA8/RGBA64 exact-Alpha handling, cross-boundary halos and sparse incremental previews.
- **0.6.0h complete:** ordered Smudge Brush with straight-RGBA/Alpha/hidden-RGB transport, editable channel and mask targets, optional Finger Painting, selection-in-dab clipping, RGBA8/RGBA64 CPU reference and incremental long-stroke preview.
- **0.6.0i complete:** compact Retouch toolbar family, single-corner family indicators, cooperative Escape cancellation for Poisson retouch work, all-or-nothing cancellation publication, bounded Healing/Patch active regions and full retouch regression/persistence hardening.
- **0.6.0i.1 complete:** named case-specific GPU parity diagnostics, bounded Clone f32/CPU tolerance, explicit native resource shutdown, disposable-helper teardown isolation and readback/compile-warning hardening.

- **0.7.0a complete:** ordinary editable initial Raster layers for new/imported documents, lossless legacy Base Image promotion, unrestricted duplicate/group/move/delete behaviour, empty transparent layer trees, version-6 persistence and Hot/Warm/Cold snapshot compatibility.
- **0.7.0b complete:** persistent per-document Crop frame; Free/Ratio/Fixed Size constraints; numeric geometry; overlays, dimming and snapping; Crop from Selection; straighten preview; non-destructive document bounds with preserved off-canvas raster/mask storage; optional destructive clipping; atomic cancellable Undo/Redo; and 8/16-bit hidden-RGB hardening.
- **0.7.0c.1 complete:** reusable Canvas Size document-bounds transaction; absolute/relative dimensions; deterministic nine-point anchors; transparent pure-bounds expansion; non-destructive contraction; exact layer/mask/storage translation; selection/guide clipping; cancellable stale-safe publication; one atomic Undo/Redo action; and version-6/Hot-Warm-Cold compatibility.
- **0.7.0c.1.1 hotfix:** prevent programmatic progress-dialog closure from being mistaken for user cancellation, allowing completed Canvas Size transactions to commit.
- **0.7.0c.2 complete:** Foreground/Background/Custom extension fills on an editable bottom Canvas Extension layer; safe transparent default; optional destructive raster/mask clipping; exact 8/16-bit Alpha and hidden-RGB handling; atomic history, persistence and stale-safe renderer publication.
- **0.7.0c.3 complete:** storage-aware Reveal All; exact Fit Canvas to Selection; finite transformed Fit Canvas to Selected Layers; hidden-layer/group and adjustment-mask handling; cancellable stale-safe atomic publication; version-6 persistence.
- **0.7.0c.4 complete:** visible-composite Trim Transparent Pixels; tolerance-based Trim by Corner Colour with four sample corners and independently enabled sides; transparent hidden-RGB equivalence; bounded cancellable analysis; optional destructive clipping; atomic history and version-6 persistence.
- **0.7.0c.5 complete:** all-or-nothing structural publication; exact selection/edit/channel/Crop stale fingerprints; same-size renderer retirement on Undo/Redo; strip/thumbnail/residency refresh; Crop cancellation-close hardening; snapshot-safe allocation guard; saturated history accounting; and Cold-residency regression coverage.
- **0.7.0d.1 complete:** Image Size pixel/percentage dialog, linked aspect ratio, alpha-safe CPU Nearest/Bilinear resampling, independent editable raster/mask scaling, sparse selection and guide scaling, document-space hierarchy scaling, atomic history, cancellation/stale guards and version-6 persistence.
- **0.7.0d.2 complete:** bounded native tiled WebGPU Nearest/Bilinear resampling for eligible 8-bit canvas/raster/mask payloads, global cross-tile source addressing, straight-RGBA hidden-colour preservation, per-payload transactional CPU fallback, cancellation and startup parity.
- **0.7.0d.3 complete:** Catmull-Rom Bicubic, Lanczos 3 and exact Area/Box reduction; anti-aliased downscaling; bounded high-quality CPU intermediates; inches/centimetres/millimetres and PPI controls; metadata-only no-resample mode; resolution-aware history, persistence and export.
- **0.7.0d.4 complete:** combined layered-output memory preflight; finite-coordinate and resample-method validation; exact post-build canvas/layer/mask/reference/transform/selection/guide verification; GPU metadata normalisation; cancellation-after-readback rejection; empty-tree and Image Size Cold-residency hardening.

- **0.7.0e.1 complete:** persistent transform sessions; Free/Scale/Rotate modes; movable pivot; adaptive numeric controls; flips/quarter turns/context commands; one atomic Apply and history-free Cancel.
- **0.7.0e.2 complete:** edge-tangent Skew, independent-corner Distort and coupled Perspective; mode-specific handles/cursors and edge/control-point coordinates; validated finite convex projective matrices; native WebGPU projective presentation with CPU/16-bit authoritative fallbacks; masks, selections and multi-layer support.
- **0.7.0e.2.1 complete:** white idle and orange pending transform overlays; pending-state recovery when reset to identity; interior double-click Apply with handle/pivot exclusion; unchanged atomic Apply/Cancel and version-6 persistence.
- **0.7.0e.3 deferred:** the Warp experiment was removed after runtime testing exposed unacceptable performance and post-Apply movement instability. It will return only after a separate renderer/session redesign.
- **0.7.0e.4 complete:** exact whole-document flips and quarter/half turns; layered hierarchy, masks, sparse selections, guides and physical-resolution updates; per-document Transform Again and Repeat Transform recipes; Transform and Duplicate; copy/paste transform values; Nearest, Bilinear, Bicubic and Lanczos 3 Apply interpolation; shortcut and context-menu polish; atomic history and version-6 compatibility.
- **0.7.0e.4.1 correction:** typed exact-image versus editable-layer repeat recipes; exact repeated quarter turns; affine repeat without aspect-ratio squashing; transform clipboard v2 with v1 compatibility.
- **0.7.0e.5 complete:** detached all-or-nothing whole-layer and selected-target Apply; finite/invertible/projective-domain validation; bounded off-canvas storage and cumulative allocation preflight; hidden-RGB 8/16-bit preservation; stale renderer retirement; version-6 project/private-cache validation; residency/save/reopen and repeated workflow hardening.
- **0.7.0e.5.1 polish:** feature-scoped GPU startup approval, deep mixed-group selective compositor fallback, clean GCC 16 transform-safety builds and compact About dialog.

The **0.7.0 Transform Expansion milestone is complete** for Free Transform, Scale, Rotate, Skew, Distort, Perspective, exact image transforms and repeat/workflow commands. **0.7.0e.3 — Warp** remains deferred for a separate redesign and is not part of the completed supported transform set.

## 0.8.0 — Colour and Tonal Workflow Expansion

- **0.8.0a complete for testing:** schema-versioned typed adjustment payloads; exact adjustment-input histogram capture; asynchronous RGBA8/RGBA64 histogram service with bounded shared caching, cancellation and stale rejection; professional RGB/per-channel Levels editor; output ranges, Auto clipping, selection scope and eyedropper sampling; CPU/WGSL processing and version-6 migration.
- **0.8.0b.1 complete for testing:** histogram-backed RGB/per-channel Curves with smooth/linear point editing; exact shared 8/16-bit tonal lookups and bounded global cache; Exposure EV/Offset/Gamma; luminance-pivot Contrast; perceptual OKLab Saturation with deterministic gamut compression; schema-2 persistence and native WGSL parity.
- **0.8.0c complete for testing:** master and six-range Hue/Saturation; adaptive Vibrance with skin-tone protection; linear-light Temperature/Tint with neutral sampling; three-way Colour Balance with Preserve Luminosity; schema-3 persistence; straight-alpha 8/16-bit CPU reference paths and feature-validated tiled WGSL parity.
- **0.8.0c.1 performance/UX hardening:** coalesced progressive adjustment previews, one snapshot per gesture, deferred settled bookkeeping, shared slider-plus-number controls and per-adjustment native GPU approval.
- **0.8.0c.2 interactive preview ordering fix:** older in-flight slider frames are invalidated at the canvas request boundary and rejected at publication, preventing adjustment previews from flickering back to stale values while retaining coalesced GPU rendering.
- **0.8.0c.3 interactive preview redesign:** removes blurred mip interaction frames, renders the complete visible level-0 viewport through one bounded native WebGPU submission/readback where eligible, atomically publishes only complete frames, and permits monotonic single-flight interaction frames to advance while newer values coalesce.
- **0.8.0d complete for testing:** Channel Mixer with RGB/monochrome matrices, six-family Black and White with optional tint, editable multi-stop Gradient Map, schema-4 persistence, exact 8/16-bit CPU lookups and feature-validated WGSL paths.
- **0.8.0d.1 hardening:** Gradient Map drag capture and stop-position slider synchronisation are robust, and the stop-colour control uses a contained theme-safe swatch.
- **0.8.0e complete for testing:** Posterise, source-selectable Threshold, embedded `.cube` 1D/3D/combined LUT adjustment with Strength, built-in/user adjustment presets, schema-5 persistence, exact 8/16-bit CPU processing, feature-validated native WGSL for display-range LUTs and bounded CPU fallback for extended-range or oversized tables.
- **0.8.0f complete for testing:** local Shadows/Highlights with tonal-width, radius, midtone-contrast and colour-correction controls; exact 8/16-bit CPU reference; two-pass native WGSL local adaptation; cumulative tile/group dependency halos; schema-6 persistence; and final adjustment, GPU, interaction, residency and hidden-RGB hardening.

The **0.8.0 Colour and Tonal Workflow Expansion milestone is complete for testing**. All sixteen adjustment types are non-destructive, maskable and group-aware, with public project format 6 retained.

## 0.9.0 — Text and Vector Foundation

- **0.9.0a.2 complete for testing:** semantic Rectangle, Rounded Rectangle and Ellipse vector layers; linked/independent document-pixel corner radii that stay visually constant under ordinary scaling; minimum-size guards; centre-pivot Inspector rotation; Photoshop-style Shape tool and Inspector; double-click primary/secondary/saved colour choosers; bounded RGBA8/RGBA64 tile rasterisation feeding the existing native compositor; masks, groups, transforms, thumbnails, Canvas Fit and Image Size integration; public project format 7 and private snapshot schema 8.
- **0.9.0b complete for testing:** semantic Line, Polygon and Star shapes; explicit endpoints, point counts, star inner radius and vertex angle; independent solid fills and complete document-pixel strokes with alignment/caps/joins/miter limit; live creation and Inspector editing; semantic vertex snapping; editable cross-document vector clipboard transfer; fill/stroke-aware bounds, thumbnails, selection coverage, transforms and Image Size; schema-1 backward compatibility and focused malformed-data/precision tests.
- **0.9.0c.4 complete for testing:** semantic Point and Paragraph/Area Text layers, requested-font preservation and fallback, tiled RGBA8/RGBA64 rendering, direct live canvas editing, multiline/Auto Height behaviour, text-aware box resizing and empty-editor size/crash hardening; public project format 8 and private snapshot schema 9.
- **0.9.0d complete for testing:** semantic open/closed Bézier paths with stable anchors and handles; Pen and Direct Selection tools; fixed-size node overlays; straight/curved creation, 45-degree constraints, node selection/movement/insertion/deletion, Corner/Smooth/Symmetric conversion, endpoint joining, path opening/closing, vector-aware snapping, exact persistence/clipboard/Image Size/residency integration; public project format 9, vector schema 2 and private snapshot schema 10.
- **0.9.0d.1.1 complete for testing:** pointer-rate path mutation without deep whole-path comparisons, per-gesture snap-target caching, transformed geometry/stroke caching across tiles, ordered coalesced interactive viewport publication, bounded semantic transform rerenders and immediate sharp pure-translation previews for vector/text layers; settled quality and all format schemas remain unchanged.
- **0.9.0e complete for testing:** guarded SVG opening and layer import; complete M/L/H/V/C/S/Q/T/A/Z path parsing with arc-to-cubic conversion; editable rectangles, rounded rectangles, ellipses, lines, polygons, polylines, paths, groups, transforms and basic text; width/height/viewBox and preserveAspectRatio handling; inherited presentation styles; full-document and selected-root export; standard SVG geometry plus exact `data-vfx` semantic round-tripping; bounded XML, element, nesting, node and coordinate validation; unsupported raster, adjustment, gradient, clip/filter and compound-path limitations reported explicitly. Public project format 9, vector schema 2, text schema 1 and private snapshot schema 10 remain unchanged.
- **0.9.0f complete for testing:** cumulative SVG import/export complexity budgets; defensive semantic/export preflight; corrected group visibility overrides, symbol handling, text fill/stroke behaviour, strict arc flags and viewport diagnostics; supported SVG blend-mode preservation; atomic active-text integration; semantic export-action gating; and expanded malformed/interoperability regressions. All public and private format schemas remain unchanged.
- **0.9.0g.4 complete for testing:** Pen Tool top bar now mirrors Shape appearance controls with shared Fill/Stroke toggles and swatches, stroke width and the full Solid/Dashed dash/cap/join/alignment/miter popup while retaining Finish/Close Path; all format schemas remain unchanged.
- **0.9.0g.3 complete for testing:** new vectors use secondary fill and primary stroke colours, Corner Tool has a placeholder icon, and editable Solid/Dashed strokes expose dash, gap, offset, cap, join and alignment controls with project/SVG/residency round-tripping; public project format 11, vector schema 4 and private snapshot schema 12.
- **0.9.0g.2.2 complete for testing:** Direct Selection Make Sharp collapses both handles into the anchor and clears live-corner data, while Make Corner remains the independent-handle conversion; all format schemas remain unchanged.
- **0.9.0g.2.1 build-fix complete for testing:** declared `ImageCanvas::selectionMarqueePreviewMode()` to match the 0.9.0g.2 implementation and test API; no behaviour or format changes.
- **0.9.0g.2 complete:** Direct Selection Make Corner/Make Smooth actions, absolute Shift-constrained 45-degree handle angles, marquee node selection in Direct Selection and Corner tools, and raster selection-preview state isolation after vector shape creation; all format schemas remain unchanged.
- **0.9.0g.1 build-fix complete for testing:** corrected the live-corner painter lambda capture; no format or behaviour changes.
- **0.9.0g complete for testing:** live non-destructive Corner Tool in the Pen/Direct Selection family; Rounded, Chamfer, Concave and Cutout styles; multi-node canvas editing and exact radius controls; semantic primitive-to-path conversion; Bake Corners; SVG visual/metadata round-tripping; public project format 10, vector schema 3 and private snapshot schema 11 with dishonest-version rejection.

The **0.9.0 Text and Vector Foundation milestone is complete for testing**. Additional UX/QoL refinement can follow before the next major milestone without reopening the core format foundation.

## 0.10.0 — Vector Workflow Improvements and Fill Tools

- **0.10.0a complete for testing:** explicit Convert Shape to Path for rectangles, rounded rectangles, ellipses, polygons, stars and lines; Layer menu, layer context menu and Direct Selection access; multi-layer atomic conversion; exact appearance/transform retention; document-space rounded-corner baking; ordinary editable nodes; no format-schema changes.
- **0.10.0b complete for testing:** endpoint-aware Pen continuation from either side; opposite-end closure; transformed open-path joining with coincident-node merging; double-click/Enter finish; live-segment Escape cancellation; start-aware Backspace; fixed-screen Continue/Close/Join/Active endpoint feedback; atomic Undo; no format-schema changes.
- **0.10.0c complete for testing:** Direct Selection and Corner Tool Arrow/Shift+Arrow document-space nudging; Ctrl+A selects all path nodes; Delete removes selected nodes safely; Escape clears node selection first; atomic Undo; imported multi-object vector siblings preserved; no format-schema changes.
- **0.10.0d complete for testing:** Swap/Reset Fill and Stroke; bounded schema-tagged Copy/Paste Appearance covering all supported fill/stroke styling; multi-layer atomic application; persisted Shape/Pen defaults including opacity; middle-mouse panning restored for Pen, Direct Selection and Corner Tool; no format-schema changes.
- **0.10.0e complete for testing:** fixed-screen hover feedback for anchors, handles, segments, Pen endpoints and live-corner controls; distinct selected/hovered styling; component-aware cursors; overlap-aware hit priority; stale-hover clearing across pan/tool/path changes; no format-schema changes.
- **0.10.0f complete for testing:** document-space Expand Stroke for width, alignment, caps, joins, miter limits and dashes; ordinary editable closed paths; fill/stroke draw-order preservation through isolated child layers; atomic multi-layer Undo and safe rollback.
- **0.10.0f.1 complete for testing:** true compound paths for disconnected dash islands and closed stroke rings; no synthetic connector spokes or seam-producing retraced edges; active-contour Direct Selection/Corner editing; project format 12, vector schema 5 and residency snapshot 13.
- **0.10.0f.2 complete for testing:** dashed Expand Stroke accepts valid dash islands even when the platform stroker emits zero-area seam debris; independent compound contours and clean closed rings remain unchanged.
- **0.10.0f.3 complete for testing:** expanded strokes persist nonzero winding so touching/overlapping dash islands remain a union instead of failing validation; SVG nonzero/even-odd fill rules round-trip; project format 13, vector schema 6 and residency snapshot 14.
- **0.10.0g complete for testing:** independent start/end stroke arrowheads with five visible styles and stroke-relative scaling; Shape/Pen/Inspector controls; appearance, Image Size, Expand Stroke, project/residency and SVG marker integration; semantic filled Arrow Shape; project format 14, vector schema 7, appearance schema 2 and residency snapshot 15.
- **0.10.0g.1 complete for testing:** centred endpoint marker geometry so shaft caps cannot blunt pointed heads; native/SVG/Expand Stroke geometry agreement; arrowhead controls omitted for closed shapes and closed paths; no schema changes.
- **0.10.0g.2 complete for testing:** endpoint cap trimming bounded to the local first/last stroke footprint, preserving all unrelated segments of winding open Pen paths for start/end markers; no schema changes.
- **0.10.0h complete for testing:** reusable vector appearance presets with full fill/stroke/dash/cap/join/alignment/miter/arrowhead payloads; save, overwrite, apply, rename and delete manager; atomic multi-layer application; Shape/Pen default application; bounded versioned user-data storage; no document or residency schema changes.
- **0.10.0i complete for testing:** raster bucket Fill for raster/Background layers, masks and editable greyscale/RGBA channels; primary/secondary colour and Alpha; 0–255 tolerance; contiguous/global matching; selection feathering; Preserve Transparency; atomic Undo; parity-gated tiled WGSL application with exact 8/16-bit CPU fallback; no schema changes.
- **0.10.0i.1 complete for testing:** corrected invalid Fill WGSL multi-component swizzle assignment and added build/runtime source guards before pipeline submission; no schema changes.
- **0.10.0j complete for testing:** Fill/Gradient toolbar family; Linear, Radial, Angle, Reflected and Diamond raster gradients; primary-to-secondary and foreground-to-transparent colour modes; reversal; transformed-target placement handles and live preview; selection/mask/greyscale/RGBA-channel support; alpha-safe 8/16-bit reference; atomic tile-delta Undo; parity-gated tiled WGSL application with full-operation CPU fallback; no schema changes.
- **0.10.0j.1 complete for testing:** coalesced asynchronous Gradient placement preview; viewport-bounded 960–1600px working tier; immediate handles; stale-frame generation guards; cancellable interactive compositor; reduced preview surfaces never replace authoritative canvas backing; no schema changes.
- **0.10.0k complete for testing:** Merge Selected Layers for contiguous sibling roots; raster-with-raster isolated alpha-safe flattening with masks/transforms/opacity/blending, hidden RGB and bounded off-canvas storage; vector-with-vector editable path consolidation with semantic-shape conversion, affine transform baking and draw-order preservation; mixed types rejected; atomic Undo/Redo; no schema changes.
- **0.10.0k.1 complete for testing:** context-click preserves selected layer sets; merged vector layers expose every contained path through object-aware Direct Selection, Corner, Pen and keyboard editing; layer appearance controls update all objects; no schema changes.
- **0.10.0l complete for testing:** bounded layer-tree, vector-node, guide and embedded-PNG decoding; identical safety gates on project load/save and structural publication; integrated RGBA64 Fill/Gradient/mask/selection/raster-merge/vector-merge save/reopen regression; consolidated SVG, clipboard, sizing, residency, Undo/Redo, large-document, alpha and GPU/CPU regression plan; no schema changes.

The **0.10.0 Vector Workflow Improvements and Fill Tools milestone is complete for testing**. Project format remains 14, vector schema remains 7, vector appearance schema remains 2 and Hot/Warm/Cold residency schema remains 15.

## 0.10.1 — LUT Correctness and Conformance

- **0.10.1a complete for testing:** deterministic hand-auditable `.cube` fixtures and authoritative vectors; independent test-only parser/evaluator; red-fastest ordering, basic 1D/3D, combined shaper/cube, domain, stepped, 8/16-bit, project, flattened-render and preset baselines; known output-clamp, ±16 parser-clamp and RGBA8 GPU-quantisation defects explicitly pinned. Also adds New from Clipboard with copied pixel size, precision, profile/resolution retention and safe sRGB fallback. No production LUT result or persistence schema changes yet.
- **0.10.1b complete for testing:** explicit header/1D/3D parser state; strict duplicate, ambiguity and row-count validation; line-numbered unsupported-directive diagnostics; recognised video-range flags rejected rather than silently misapplied; finite table/domain values preserved without the old ±16 clamp; asymmetric red-fastest fixture; domain-source metadata persisted in adjustment schema 7 with schema 1–6 migration; project format 14 and residency schema 15 unchanged.
- **0.10.1c complete for testing:** authoritative scalar evaluator with explicit 1D-shaper then 3D-table staging, exact per-channel domains, deterministic trilinear and tetrahedral interpolation, fixed tie precedence, Strength blending against the original input and no final scalar `[0,1]` clamp; new imports default to tetrahedral CPU evaluation; schema 8 persists interpolation while schemas 1–7 migrate to trilinear; legacy trilinear RGBA8 GPU parity remains isolated and unchanged.
- **0.10.1d complete for testing:** persisted Encoded document values, Linear sRGB / Rec.709 and Raw component processing contracts; extended-range transfer conversion around the authoritative table evaluator; Strength blending after return to document components; explicit RGBA8/RGBA64 destination clamping; document-transfer-aware selective GPU fallback; unsupported ICC working-space warning without hidden gamut conversion; adjustment schema 9 with schemas 1–8 migrating to encoded compatibility.
- **0.10.1e complete for testing:** persisted Generic, Tony McMapface and AgX Base sRGB operator profiles; fresh-import filename/title suggestion plus manual selection; exact named linear-Rec.709 preprocessing/output contracts around tetrahedral sampling; adjustment-local authoritative CPU fallback; unsupported-profile and reference-size diagnostics; schema 10 with schemas 1–9 migrating to Generic; project format 14 and residency schema 15 unchanged.
- **0.10.1f complete for testing:** direct RGBA16Float LUT upload without QImage/8-bit quantisation; explicit WGSL trilinear and tetrahedral interpolation; native encoded/linear/raw processing plus Tony and AgX pipelines; multi-case startup CPU/GPU parity gate; binary16/f32/dimension CPU fallback; single authoritative checked-in adjustment shader; no schema changes.
- **0.10.1g complete for testing:** full Inspector contract and diagnostics; editable Generic interpolation; exact domain/source display; actionable GPU/CPU status and Copy Details; duplication/project/preset/Hot-Warm-Cold persistence coverage; oversized-preset preflight and explicit replacement confirmation; no schema changes.
- **0.10.1g.1 build fix:** resolves the GCC ambiguity between the Inspector-local and shared LUT domain-source display helpers; no behaviour or schema changes.

Current agreed roadmap: **0.11.0 Colour Management Foundation**, **0.12.0 Presets and Production Export Foundation**, **0.13.0 Adjustment and Image Filter Expansion**, **0.14.0 Smart Layers and Live Filters**, **0.15.0 Camera and Lens Corrections**, **0.16.0 ColorChecker and Colour Calibration**, **0.17.0 Filters and Effects Expansion**, **0.18.0 Content-Aware Retouching**, and **0.19.0 Batch Processing and Automation**.


## 0.11.0 — Colour Management Foundation

- **0.11.0a complete:** explicit input/working/display/proof/output colour state; stable ICC/OCIO-ready descriptors and transform identities; central Qt transform cache; project format 15; residency snapshot 16; versions 1–14 migrate to Legacy V1 without pixel conversion or visible changes; colour-state-aware tile/preview/histogram/thumbnail cache keys; dedicated deterministic migration and persistence tests.
- **0.11.0b complete for testing:** bounded ICC-aware PNG/JPEG/TIFF ingress; exact usable-profile metadata/fingerprints; consistent Assume sRGB / Ask / Leave Untagged policy across file open, drag/drop and clipboard paths; schema-2 persistence; document diagnostics; damaged-profile warnings; no file-open/new-document pixel conversion, while existing cross-profile clipboard conversion is retained.
- **0.11.0c complete for testing:** built-in and external ICC working-space selection; Assign Profile metadata-only interpretation; transactional/cancellable Convert to Profile for RGBA8/RGBA16 raster and semantic vector/text/Gradient Map colours; Alpha/mask/selection preservation; colour-aware structural Undo/Redo; project and residency retagging; legacy projects migrate only after explicit user action.
- **0.11.0d complete for testing:** explicit adjustment-domain contracts; managed linear-working Exposure; explicit sRGB/Rec.709 adaptation for established colour/luminance operators; raw-component and LUT isolation; central transform-cache reuse; Legacy V1 render protection; processing-context propagation through previews, histograms, export, merge and trim; selective temporary CPU fallback; deterministic 8/16-bit and wide-gamut conformance tests.
- **0.11.0e complete:** optional OpenColorIO 2.5.2 integration; built-in ACES 2 CG/Studio, environment and external config selection; colour-state schema 3 persistence; safely representable ACES/Rec.709 working spaces in Assign/Convert; Qt matrix/TRC proxies that preserve 0.11.0d adjustment-domain correctness; ICC bridge; display/view validation; missing/change fingerprint protection; CPU conformance tests.
- **0.11.0f complete for testing:** presentation-only working→proof→display CPU pipeline; Linux colord and Windows WCS monitor ICC discovery; manual/environment override and sRGB fallback; screen-move refresh; explicit OCIO DisplayView processing; soft-proof profile, intent/BPC request and deterministic gamut-warning groundwork; parallel canvas/tile/live/transform presentation caches and derived document-strip thumbnails; schema-4 legacy gate; project/residency persistence and no-Undo display changes.
- **0.11.0g complete for testing:** deterministic 65³ half-float reference lattices for ICC, OCIO Display/View, soft proof and managed working↔adjustment-domain transitions; RGBA8/RGBA16 presentation evaluation; tiled managed linear-working Exposure and encoded-sRGB/Rec.709 operator integration; exact Alpha preservation; per-transform CPU-reference probes; bounded shared CPU/GPU caches; independent display and managed-adjustment startup parity gates; feature-specific CPU fallback; no monitor/proof transform in document/export pixels.
- **0.11.0h complete for testing:** explicit full-resolution working→output ICC/OCIO conversion; format-aware ICC metadata; PNG/TIFF 16-bit integer output; deterministic RGB-only 64×64 blue-noise 16→8 reduction; exact endpoint and Alpha handling; output-space matte flattening; project/residency output defaults without Undo or processing-cache invalidation; monitor/proof/display isolation and deterministic export tests.
- **0.11.0i complete for testing:** immutable external ICC/OCIO resource audit; embedded-ICC authority; no silent substitution; separation of runtime resource warnings from project repairs; Cold-residency re-audit; strict fingerprint/embedded-profile/base64 validation; idempotent presentation/output metadata; final legacy, multi-document, Alpha/hidden-RGB, GPU/CPU fallback, export and cancellation conformance coverage. **0.11.0 Colour Management Foundation complete.**
- **0.11.0i.1 hotfix for testing:** bounded reusable Smudge pre-dab scratch storage, constant-time dirty bounds, explicit writable-format normalisation, removal of redundant effect-brush selection work, helper-to-GUI GPU capability transfer without duplicate parity execution, worker draining and canvas-before-GPU shutdown ordering.
- **0.11.0i.2 hotfix for testing:** removes incremental full-frame `QImage` detaches, carries Smudge spacing across raw events, skips no-op preview composites, reuses full-resolution live results at release, halves transported bilinear sampling work, limits history generation to path-touched tiles and reports commit/history timings.
- **0.11.0i.3 blocker for testing:** destroys obsolete toolbar-owned `QWidgetAction` generations during each tool-options rebuild, hardens Smudge/effect-brush direct image writes, rejects undersized ICC inputs before Qt parsing and adds ASan/UBSan build and test presets.


## 0.12.0 — Presets and Production Export Foundation

- **0.12.0a complete:** unified versioned preset envelopes, stable built-in/user identities, metadata, atomic storage and legacy-safe adjustment/vector migration.
- **0.12.0b complete:** shared searchable preset manager with complete user CRUD, import/export, categories, tags, favourites and recents.
- **0.12.0c complete:** reusable export profiles and validated portable filename templates while retaining the approachable single-image export path.
- **0.12.0d complete:** failure-isolated multi-output production plans, per-output resize/naming/profile snapshots, collision policies and atomic writes.
- **0.12.0e complete:** bounded serial modeless Export Queue with immutable multi-document snapshots, progress, pause/resume and cancellation.
- **0.12.0f complete:** atomic private recovery descriptions, non-writing startup restoration, fresh collision review, quarantine and preserve-on-quit.
- **0.12.0g complete for testing:** strict plan/payload validation, side-effect-free enqueue rejection, live Skip decisions, exact progress, completed-worker shutdown classification, iterative recovery validation, storage preflight and atomic-write hardening. **0.12.0 Presets and Production Export Foundation complete.**

Folder-wide batch processing and automation remain **0.19.0**.

## 0.13.0 — Adjustment and Image Filter Expansion

- **0.13.0a complete for testing:** shared radius-aware spatial-filter contract; tile halos and dependency planning; Clamp/Mirror/Wrap/Transparent edges; inverse dirty-region expansion; stable C++/WGSL packing; exact RGBA8/RGBA64 CPU fixtures; hidden-RGB and straight-Alpha contracts; cancellation, memory limits, deterministic cache fingerprints and tiled/full-frame tests. Existing Shadows/Highlights now reuses the shared 13-tap CPU kernel while retaining its established GPU parity gate. No public filter collection or schema change yet.
- **0.13.0b complete for testing:** public Gaussian Blur, Box Blur, Unsharp Mask and High Pass adjustment layers; append-only adjustment schema 11; deterministic three-box Gaussian approximation; exact Box Blur; stable 8-bit threshold units; coverage-aware or preserved Alpha; hidden-RGB-safe sharpening; built-in presets; cumulative halo/tile-cache integration; cancellation and full-frame/tiled tests. New filters use the deterministic CPU reference until dedicated WGSL kernels pass the existing per-feature parity gate.
  - **0.13.0b.2 performance blocker:** parallel CPU row/column/component scheduling and mip-scaled interaction previews, with unchanged level-0 final output and schemas.
  - **0.13.0b.3 interaction blocker:** retain and atomically replace complete spatial interaction mips across render generations, preventing fallback-image flicker before the level-0 release render.
  - **0.13.0b.4 preview-correctness blocker:** keep Unsharp Mask and High Pass interactions at full-resolution level 0, retain atomic frame replacement and reuse the latest completed sharpen generation on release; Blur may continue using its responsive mip preview.
- **0.13.0c complete for testing:** append-only Invert and Photo Filter adjustment layers; adjustment schema 12; straight-RGB/hidden-RGB-safe Invert; managed encoded-sRGB/linear-Rec.709 Photo Filter with Density, Preserve luminosity, standard warming/cooling colours and built-in presets; CPU/WGSL and managed-domain parity gates; persistence, region, 8/16-bit and Cold-restoration coverage. Existing Vibrance, Threshold and Posterise contracts remain unchanged.
- **0.13.0d complete for testing:** Selective Colour appended as adjustment ID 22 and schema 13 with nine chromatic/tonal ranges, CMYK-style controls and Relative/Absolute methods; shared colour-family/range infrastructure; CPU/WGSL and managed-domain parity gates; cache, project, preset, Hot/Warm/Cold and immutable export integration; expanded Colour Balance, Channel Mixer, Black & White and Selective Colour presets; Channel Mixer output-total UX and range-switch signal hardening.
- **0.13.0e complete for testing:** append-only Vignette, creative RGB Split and manual radial Chromatic Aberration Correction adjustment layers; schema 14; document-relative vignette geometry and tint; bilinear channel displacement with radius-aware halos; exact multicore 8/16-bit CPU references, Alpha-safe hidden RGB, cancellation, presets, cache/persistence/export integration and deterministic region tests. Profile-driven lens correction remains 0.15.0.
- **0.13.0g.14 in-between-milestones fix-up:** Vignette Size 10–400% with schema-16 legacy migration, corners-only geometry, faithful on-canvas start/radius/falloff guides, outside-document centre/size/midpoint/rotation interaction, grouped Undo and a Corners Only preset. Smart Layers remain 0.14.0.
- **0.13.0g.14.1 build correction:** replaces the new Vignette overlay MOC signal bridge with owner-cleared callbacks so incremental Fedora builds link without changing the 0.13.0g.14 feature contract.
- **0.13.0g.15a vector Feather foundation:** adds schema-8 per-vector-layer Feather state, project format 16, Hot/Warm/Cold snapshot 17, Inspector scrub/type editing, grouped Undo, persistence/copy/duplication/cache identity and explicit same-value merge compatibility. Exact CPU feather rendering remains 0.13.0g.15b; Smart Layers remain 0.14.0.
- **0.13.0g.15b exact CPU vector Feather renderer:** preserves the exact zero path, feathers one combined fill/stroke silhouette with the deterministic three-box reference kernel, propagates authored edge colour without RGB blur, supports fractional/large/off-canvas radii, RGBA8/RGBA64 and tile/full equality. It remains the authoritative fallback and parity reference; Smart Layers remain 0.14.0.
- **0.13.0g.15c native tiled GPU vector Feather:** adds exact horizontal/vertical WGSL coverage convolution, startup CPU/GPU parity gating, scaled tile halos, off-canvas contribution, local dirty/cache identity and honest full-quality CPU fallback for unsupported radii, 16-bit documents or unavailable/unapproved GPU execution.
- **0.13.0g.15d vector Feather workflow integration:** uses rendered Feather bounds for canvas-fit/reveal, scales Feather with Image Size, preserves one combined silhouette through Expand Stroke, rejects lossy non-zero editable vector merges, confirms shared masks/groups/selections/residency/flattened-export integration, and preserves exact VFX SVG round-trip metadata with an explicit external-viewer warning instead of approximation or rasterisation.
- **0.13.0g.15e vector Feather hardening:** closes the mini-milestone with exact zero-return RGBA8/RGBA64 coverage, tiny/maximum-radius and stroke/winding regressions, bounded scrub-cache checks, conservative GPU preparation memory accounting and pre-version-17 residency rejection. Vector schema 8, project 16 and residency 17 stay unchanged. **0.13.0g.15 is complete; 0.13.0g.15e.1 removed redundant tiled Feather work, and 0.13.0g.15e.2 removes the remaining synchronous semantic/display-conversion work from ordinary affine pointer events by using the raster-style transform proxy. Smart Layers remain 0.14.0.**
- **0.13.0f complete for testing:** append-only Surface Blur, angle-aware Motion Blur and bounded Spin/Zoom Radial Blur adjustment layers; schema 15; anisotropic cumulative X/Y tile halos; deterministic multicore 8/16-bit references; stable Surface threshold units; optional coverage-aware Alpha for directional/radial blur; hidden RGB, cancellation, presets, cache, persistence, residency and immutable export integration.
- **0.13.0g complete for testing:** Curves on-image sampling and Reset All; visual/sampleable/resettable Hue/Saturation ranges; Gradient Map duplication/distribution/keyboard editing; expanded presets; bounded multicore exact histograms; one-shot adjustment-eyedropper isolation across Inspector and Hot/Warm/Cold transitions; deterministic editor/histogram tests; full schema/export/queue/recovery compatibility review. **0.13.0 Adjustment and Image Filter Expansion complete.**
- **0.13.0g.1–g.8 fix-up:** bottom zoom controls; one global snapping toggle; half-pixel guide placement; integer-boundary vector creation/anchor editing; whole-layer Transform pixel snapping; asynchronous transform-preview preparation; dedicated 24 × 24 tool icons; aligned colour controls; fixed-height scrub fields and Inspector stability; and cache-free incremental live paint composites beneath adjustment stacks. No 0.14.0 work begun.

## 0.14.0 — Smart Layers, Live Filters and Core Layer Effects

- **0.14.0a complete:** explicit Smart Layer instance type; document-owned Smart Source UUID/revision registry; Embedded/Linked source descriptors; bounded acyclic dependency graph; transitive revision invalidation; structural Undo state and cache identity.
- **0.14.0b complete:** authoritative embedded Smart Source layer documents; structural single/multi/group Convert to Smart Layer; nested-source dependencies; exact revision-keyed presentation rendering; Smart thumbnails; public project format 18; Hot/Warm/Cold snapshot format 19; exact structural Undo state and persistence.
- **0.14.0c complete:** Edit Contents/double-click opens embedded Smart Sources as ordinary multi-document sessions; branch-style source editing; transactional Save into the immediate owner; dependency-first transitive revision/presentation propagation; stale-editor and cycle rejection; parent structural Undo; source-editor Hot/Warm/Cold bindings; schema-2 8/16-bit precision metadata; explicit source-working → containing-working colour-managed presentation boundary; public project format 19; private snapshot format 20.
- **0.14.0d complete for testing:** source-backed Smart instance transforms remain separate from authoritative contents; repeated transforms never bake intermediate Smart pixels; persistent Nearest/Bilinear/Bicubic/Lanczos sampling; straight-RGBA64 hidden-RGB-safe exact CPU references; matching Smart-mask sampling; region-footprint/halo preparation; matrix-only live transform proxy; source-edit-under-transform regression coverage; public project format 20, embedded source schema 3, private snapshot format 21.
- **0.14.0e complete for testing:** 256×256 straight-RGBA64 Smart source tiles; transform-aware inverse footprints/halos; bounded source/transformed/mask intermediate LRU; exact source-region dirty fingerprints; tile-local Smart composite invalidation; cross-revision reuse for pixel-identical source footprints; native WebGPU Smart-tile residency reuse; runtime-only caches with project 20 / embedded schema 3 / snapshot 21 unchanged.
- **0.14.0f complete for testing:** persistent per-Smart-instance Live Filter stacks with stable identity/revisions; all existing typed 0.13 operators reusable as Live Filters; exact CPU stack at transform→filters→mask; cumulative off-canvas spatial halos; prefix-stage Smart cache reuse; tile/GPU-residency semantic invalidation; project 21 / embedded schema 4 / snapshot 22 persistence and migration.
- **0.14.0g complete for testing:** Smart Layer Live Filter pseudo-rows and Add Filter menus; full reuse of the mature adjustment Inspector editors with grouped parameter Undo; transform-aware per-filter masks with selection round-trip editing; Live Filter-aware Levels/Curves histograms and on-image sampling; parity-gated native WebGPU adjustment passes with exact CPU fallback for unapproved/spatial stacks; project 22 / embedded schema 5 / snapshot 23 / Live Filter schema 2 persistence and migration.
- **0.14.0h complete for testing:** distinct persistent Layer Effect (`fx`) stacks on Raster/Vector/Text/Smart layers; Layers-panel fx pseudo-hierarchy and Inspector/context authoring; exact unclipped hidden-RGB content + mask-aware coverage input contract; disabled-definition guard until concrete renderers land; project 23 / embedded schema 6 / snapshot 24 / Layer Effect schema 1 persistence and migration.
- **0.14.0i complete for testing:** exact non-destructive Drop Shadow, Inner Shadow, Outer Glow and Inner Glow from full-precision mask-aware coverage; editable colour/opacity/blend/angle/distance/Spread-or-Choke/Size; independent behind/front passes; bounded fx intermediate LRU; effect-expanded tile fingerprints; native WebGPU pass compositing/residency reuse; project 24 / embedded schema 7 / snapshot 25 / Layer Effect schema 2 persistence and migration.
- **0.14.0j complete for testing:** exact mask-aware Layer Effect Stroke with Inside/Centre/Outside placement; Colour Overlay; full Gradient Overlay with editable stops/interpolation, Linear/Radial style, angle/scale/reverse; stable owner-space tiled gradient coordinates; existing bounded fx cache/native pass residency integration; project 25 / embedded schema 8 / snapshot 26 / Layer Effect schema 3 persistence and migration.
- **0.14.0k complete for testing:** signed-distance-field Bevel & Emboss with Inner/Outer/Emboss/Pillow styles, Up/Down direction, Depth/Size/Soften, Angle/Altitude lighting, independent Highlight/Shadow blend passes, bounded large-export streaming, project 26 / embedded schema 9 / snapshot 27 / Layer Effect schema 4 persistence and migration.
- **0.14.0l complete for testing:** explicit externally linked `.vfxphoto` Smart Sources with persistent document identity and composite dependency fingerprints; open-document source-save propagation; nested links and strict cross-file cycle rejection; safe missing-link cache retention; Replace Source, Relink Source and Embed Linked Source; relative-link Save As rebasing; selective source/dependent revision invalidation; project 27 / Smart Source schema 3 / embedded schema 10 / snapshot 28 / Layer Effect schema 4 persistence and migration.
- **0.14.0m complete for testing:** milestone-wide Smart/Live Filter/`fx` integration hardening; selective transitive open-document linked-source propagation; accepted-export linked-source preflight; linked-warning/history restoration fixes; project-27 test cleanup; integrated 16-bit Smart + filter-mask + fx + layer-mask persistence/render regression coverage. Persistence remains project 27 / Smart Source schema 3 / embedded schema 10 / snapshot 28 / Layer Effect schema 4.

The 0.14.0 architecture keeps Smart Sources, transforms, Live Filters and Layer Effects semantically distinct even where they share tiled cache/invalidation infrastructure. There are no further planned implementation stages in 0.14.0; remaining work is Fedora/manual regression, performance and QoL feedback plus fixes discovered during that gate. **0.15.0 remains blocked until 0.14.0 is accepted.**

